package core

import math.vec3
import kotlin.math.PI

object Constants {
	
	val pi: Float = PI.toFloat()
	
	val RED = vec3(1.0f, 0.0f, 0.0f)
	val GREEN = vec3(0.0f, 1.0f, 0.0f)
	val BLUE = vec3(0.0f, 0.0f, 1.0f)
	val BLACK = vec3(0.0f, 0.0f, 0.0f)
	val WHITE = vec3(1.0f, 1.0f, 1.0f)
}